if [[ -d $HOME/.zsh ]] then
 zdotdir=${ZDOTDIR:-$HOME/.zsh}
 export ZDOTDIR="$zdotdir"
fi
# to be sure
source $ZDOTDIR/.zshenv
#if [ -e /Users/mars/.nix-profile/etc/profile.d/nix.sh ]; then . /Users/mars/.nix-profile/etc/profile.d/nix.sh; fi # added by Nix installer
